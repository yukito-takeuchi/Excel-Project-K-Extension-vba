# 黒永様 財務レポート自動化システム - 自律実行管理ファイル

## 参照ドキュメント（毎セッション必読）
- **要件定義書** → `docs/要件整理.md`

> 実装判断に迷ったら必ず上記を参照。要件定義書に記載のない機能は追加しない。

## プロジェクト概要
FreeWay（会計ソフト）のCSV → 複数Excelファイルへ自動取り込み＋財務グラフ自動生成のデモUI実装。

**最重要制約：`*.xlsm` / `*.xlsx` の既存Excelファイルは一切変更しない。**
**編集対象：`demo/index.html` のみ（VBAマクロは実装完了済み・触らない）**

## ファイル構成
```
vba/                 ← git管理ルート（ここ）
├ CLAUDE.md          ← 本ファイル（タスク管理の唯一の正）
├ docs/要件整理.md   ← 要件定義書
├ CSV_Import.bas     ← 英語版（参考のみ・編集不要）
├ CSV自動取込.bas    ← 実装完了（307行）。編集不要
└ demo/
  └ index.html       ← 編集対象（現在764行）
```

## demo/index.html の現状
```
現在の画面フロー：
  screen-0（CSV取込）→ screen-1（4月次収益分析）→ screen-2（11CF）
  → screen-3（12決算書）→ screen-4（完了）

現在のシートタブ：4つ（stab0/stab1/stab2/stab3）※IDにハイフンなし
現在のSTEPドット：6個（dot0〜dot5）、id="dot0"〜id="dot5"
現在の goSheet()：数値引数（0〜4）で動作（L713〜L729）

追加する画面：
  screen-1b（1表紙）/ screen-1c（2月別推移）/ screen-1d（3三期比較）
  screen-1e（5三期決算）/ screen-1f（6経営計画）/ screen-1g（10CF推移）

完成後の画面フロー：
  0 → 1b → 1c → 1d → 1 → 1e → 1f → 1g → 2 → 3 → 4
```

## 開発ルール（必読・全セッション共通）
- `demo/index.html` は**単一ファイル完結**。外部ライブラリ・ファイル分割禁止
- グラフは **SVGインライン or CSSバー** で実装（Chart.js等禁止）
- 既存CSSクラス（`btn-green` / `btn-blue` / `section` / `section-title` / `excel-table` / `prog-wrap`）を流用
- `runSteps()` 関数（プログレスバー共通）を再利用する
- モックデータは財務らしい数字（売上1,000〜5,000万円台）で可
- **タスク完了後は本ファイルの `[ ]` を `[x]` に更新する**
- **セッション終了時は「引き継ぎメモ」に作業内容を追記する**

---

## ⚡ セッション開始プロトコル（必読・毎回実行）

```
1. このファイルを先頭から最後まで読む
2. タスクキューから未完了（[ ]）のタスクを上から1つ選ぶ
   ※ B-1a → B-1b → … の順番を守る
3. 作業開始前にタスクを in_progress 状態にする
4. タスク完了後：
   a. [ ] を [x] に変更
   b. 「完了ログ」に1行追記
   c. 次のタスクへ進む
5. 作業終了時：「引き継ぎメモ」に状況を追記
```

---

## タスクキュー

### WS-A：VBAマクロ（`CSV自動取込.bas`）

- [x] A-1〜A-6：全実装完了（307行）

---

#### WS-A 残タスク（コード品質・Windows互換）

**実装可能（Claude Codeで即実施）**

- [x] A-7：`ErrHandler` に `GoTo Cleanup` を明示追加（2026-04-15）
- [x] A-8：`Chr(13)` を `vbCrLf` に全置換（Windows互換性向上）（2026-04-15）
- [x] A-9：Sub名を `CSV_Import` → `CSV取込` に変更（2026-04-15）

---

**要確認（Windows実機テストが必要・Claude Codeでは実施不可）**

- [ ] T-1：実際の `0全科目月次ﾃﾞｰﾀ出力.xlsm` でシートインデックスを確認
  - 確認内容：Sheet(1)=三期分 / Sheet(4)=当期のみ / Sheet(6)=税込データ専用 が正しいか
  - 手順：Excel を開く → シートタブを右クリック → 「シートの移動またはコピー」でインデックス確認
  - 不一致の場合：`CLAUDE.md` の定数値を修正して A-7 を再実施

- [ ] T-2：Windows環境でマクロの動作確認
  - チェックリスト：
    - [ ] 税抜方式：当期CSV → Sheet(4) に正しく貼り付けられるか
    - [ ] 税抜方式：税込CSV → Sheet(6) に正しく貼り付けられるか
    - [ ] 税込方式：当期CSVのみ取り込み、Sheet(6) は変更されないか
    - [ ] 三期分スキップ：Sheet(1) が変更されないか
    - [ ] キャンセル時：データが消えないか
    - [ ] 完了後：取り込み行数が表示されるか

- [ ] T-3：FreeWay出力CSVの文字コードを確認
  - 確認内容：Shift-JIS か UTF-8 か
  - Shift-JIS であれば現状コードで問題なし
  - UTF-8 の場合：A-10（`Workbooks.Open` に `Origin` パラメーター追加）を追加実施

---

**オプション（クライアント確認後に実施）**

- [ ] A-10：UTF-8対応（T-3でUTF-8と判明した場合のみ）
  - `Workbooks.Open(csvPath)` → `Workbooks.Open(csvPath, , , , , , , xlWindows)` に変更
  - 備考：`Origin:=65001` でUTF-8を明示できるが、FreeWayがShift-JIS出力なら不要

- [ ] A-11：マクロ実行ボタンをExcelシートに設置（クライアント希望の場合）
  - Sheet(4)「602当期のみ」の目立つ位置に「▶ CSV取込」ボタンを設置
  - 開発者タブ → 挿入 → ボタン → `CSV取込` マクロを割り当て

---

### WS-B：デモUI（`../demo/index.html`）

> B-1a → B-1b → B-1c → B-1d → B-1e → B-1f → B-2 → B-3 の順で実行

- [x] B-1a：SCREEN 1b「1表紙」追加
- [x] B-1b：SCREEN 1c「2月別推移グラフ」追加
- [x] B-1c：SCREEN 1d「3売上三期比較グラフ」追加
- [x] B-1d：SCREEN 1e「5三期比較決算図表」追加
- [x] B-1e：SCREEN 1f「6経営計画・予実グラフ」追加
- [x] B-1f：SCREEN 1g「10キャッシュ推移表」追加
- [x] B-2：goSheet()・シートタブ・STEPドット 全体改修
- [x] B-3：完了画面の改修（全ファイルリスト・PDFボタン）

---

## 判断ルール集

| 場面 | 判断ルール |
|------|-----------|
| グラフ実装 | Chart.js等禁止。SVGインライン or CSSバーで実装 |
| モックデータ | 財務らしい数字（売上1,000〜5,000万円台）なら何でもよい |
| CSSスタイル | 既存クラスを最大限流用。新規クラス追加は最小限 |
| 画面IDの命名 | `screen-{ファイル番号}` パターン（例：`screen-1b`） |
| プログレスバー | `runSteps()` を再利用。prog要素IDは `prog-{screenId}-wrap/fill/txt` |

---

## 完了ログ

| 日付 | タスク | 概要 |
|------|--------|------|
| 2026-04-14 | A-1〜A-5 | VBAマクロ全面実装。消費税方式分岐・エラーハンドリング・UX改善 |
| 2026-04-15 | A-6 | 文字コードエラー案内を PasteError ハンドラに追加 |
| 2026-04-15 | A-7〜A-9 | ErrHandlerにGoTo Cleanup明示・Chr(13)→vbCrLf全置換・Sub名をCSV取込に変更 |

---

## 引き継ぎメモ

### 2026-04-15 WS-B 全タスク完了

**変更ファイル：** `../demo/index.html`（764行 → 1,260行超）

**完了内容：**
- screen-1b（1表紙）：会社名・会計期間・担当者入力フォーム
- screen-1c（2月別推移グラフ）：12ヶ月CSSバー・生成ボタン・プログレスバー
- screen-1d（3売上三期比較グラフ）：3期分CSSバー（前々期/前期/当期）
- screen-1e（5三期比較決算図表）：PL/BS 3期横並びテーブル
- screen-1f（6経営計画・予実グラフ）：計画vs実績SVG折れ線グラフ（2本）
- screen-1g（10キャッシュ推移表）：CSSバー推移グラフ＋運転資金倍率テーブル
- goSheet() を SCREEN_ORDER/FNAMES マップ対応の文字列ID版に全面書き直し
- シートタブを10タブに拡張（stab-0〜stab-3）
- STEPドットを10個に拡張（dot0〜dot9）
- 完了画面を全9ファイルリスト＋PDFボタン対応に更新
- gen1c/gen1d/gen1e/gen1f/gen1g の新JS関数を追加
- resetDemo() を新画面のリセット処理に対応

**全タスク完了。以降の変更不要。**
